# Capital Command mutation-origin security repair

Branch: security/local-mutation-origin, isolated clone /workspace/scratch/cc6a72196596/capital-command. No commit/push/deployment. Read AGENTS.md, applicable CLAUDE.md update/voice rules and Next.js skill. No release script, scheduled job, account token or provider contacted.

Confirmed high-impact local CSRF exposure: src/app/api/update/route.ts originally exported POST() with no request validation before startRelease; src/app/api/voice/session/route.ts POST parsed even text/plain JSON and called createVoiceSession. A foreign browser page could send a simple cross-origin request that reaches these side effects despite inability to read the response. DELETE also lacked origin defense. Exact severity depends on local browser reachability and available release/provider configuration; this is not a remote network authentication fix.

Changes:
- src/lib/security/mutation-origin.ts: reusable fail-closed browser mutation guard. Rejects null/foreign/malformed origins, serialized URL paths, different ports, deceptive hostname suffixes and non-same-origin Sec-Fetch-Site. Permits exact same-origin requests, same-origin iframe requests and direct CLI requests without browser origin/fetch headers.
- src/app/api/update/route.ts: checks guard before status read and release logic.
- src/app/api/voice/session/route.ts: checks guard before POST body/provider work and DELETE revokeGrant.
- scripts/security-origin.test.mjs: executes actual TypeScript routes using Node 24 native type stripping and resolver hooks. Next response, schema, release and provider modules are fully mocked. No Next server, release process or provider operation occurs. Zod validation is deliberately outside this test scope.
- CHANGELOG.md: Unreleased user-facing entry.
- src/lib/release/update-route.test.ts: adapted both existing handler calls to same-origin Request inputs, retaining original retry200/startRelease and active409/no-start assertions.

Verification loop:
1. node --test scripts/security-origin.test.mjs: 3 tests pass. Malicious matrix: 8 header scenarios * 3 mutation handlers = 24 denied calls and zero side effects. Legitimate matrix: 4 caller scenarios * 3 handlers = 12 successful mocked operations.
2. Original route bytes copied via git show HEAD:<path> into separate capital-baseline-proof. Same test: malicious test fails (200 instead of 403), legitimate test passes. Final fixed run remains 3/3 green. The helper and host-normalization case also pass with the baseline routes; the malicious route test still fails.
3. Initial npm run typecheck in full clone blocked (tsc not found). Continued loop: installed exact locked versions into isolated capital-integration fixture with --ignore-scripts --no-audit --no-fund: Next 15.5.18, TypeScript 5.9.3, Zod 3.25.76, React/ReactDOM 19.2.6, @types/node 22.19.19 and @types/react 19.2.14. 27 packages only, no native large-app dependencies.
4. Copied actual changed route and guard bytes into minimal fixture. Production release/provider dependencies replaced by inert typed stubs. Actual Next server binds 127.0.0.1:3217. node run-http-proof.mjs performs real HTTP requests with custom Host/Origin/Fetch headers: 36 checks pass (24 denials, 12 legitimate). Includes localhost browser Host against configured 127.0.0.1 server, loopback literal, same-origin iframe, direct CLI, foreign and opaque Origin, DNS rebinding domain, altered browser/Host ports and same-site/cross-site Fetch headers.
5. Substituted exact original routes from git HEAD into fixture; real HTTP proof fails first foreign POST: response 200 and mocked release started, expected 403. Restored final routes; ./node_modules/.bin/tsc --noEmit exits 0 with actual Next/Zod/React types, then HTTP proof again passes all36. No side effect left fixture. Baseline log: capital-integration/baseline-http.log. Reproduction harness: capital-integration/run-http-proof.mjs and http-proof.mjs. Server stopped at end of each run.

Parent independent review identified a hostname-normalization compatibility risk. Revised the guard to use validated incoming Host authority, permitting different loopback aliases while enforcing equal ports; added a third test for alias preservation, malformed Host, different port and rebinding-domain rejection.

Review challenges blocked: foreign-origin text/plain form-compatible request; opaque Origin:null; alternate localhost port; attacker hostname suffix; Origin with a path; no Origin with cross-site/same-site Fetch header; contradictory same-origin Origin plus cross-site Fetch header. Legitimate same-origin iframe and headerless local CLI retained.

Limits: guard is not authentication; non-browser clients can omit browser headers by design, so local binding remains required. The revised guard requires loopback URL and Host authorities and equal ports; same-origin rebinding domains are rejected. Other mutation routes remain outside this patch. Real Next hostname normalization is now verified via the isolated HTTP server with actual copied routes and inert side effects. Full project dependency/typecheck gate and Windows production validation remain unverified; scoped route/guard TypeScript gate passes. Independent final review by parent remains pending. Patch quality 9/10 (effectiveness 3/3, regressions 3/3, compatibility 1.5/2, review evidence 1.5/2), conditional on parent final review. Deductions reflect isolated-provider/release stubs and lack of full-app/Windows verification. Do not report app-wide 9/10.


## Additional peer-review repair loop

Peer bridge_review found the old update-route tests still invoked POST() without Request. Fixed both call sites with genuine same-origin localhost Request objects; searched src/scripts test imports for both changed routes and found no other affected direct calls. The peer independently reviewed the correction and confirmed original behavior assertions were retained.

Installed exact locked Vitest 3.2.4 in isolated dependency fixture, scripts disabled. Separate capital-release-tests copies real release modules, changed update route, guard and repository's hermetic test setup/config. Existing release start mock prevents any release process; temporary data paths keep tests isolated.

Command: ./node_modules/.bin/vitest run src/lib/release/update-route.test.ts src/lib/release/shared.test.ts src/lib/release/run.test.ts src/lib/release/progress.test.ts src/lib/release/restart.test.ts src/lib/release/refs.test.ts

Result: exit0, 6 files and 62 tests passed (update-route2, shared33, run4, progress6, restart5, refs12). This validates applicable existing behavior, not a full-project suite or broad typecheck.

Final independent peer approval received in capital-peer-review.md: 9/10 scoped to the three guarded handlers. Peer independently reran corrected update-route tests (2/2 pass) and challenged 15 additional real Next HTTP calls (12 hostile denials and 3 legitimate IPv6 successes). Final status: approved scoped repair; no app-wide security claim.


## Permanent default-suite coverage

Added src/lib/security/mutation-origin.test.ts (17 guard cases) under the existing src/**/*.test.ts Vitest discovery glob. Extended src/lib/release/update-route.test.ts with 2 hostile-Origin refusal cases proving the release starter remains uncalled, alongside unchanged legitimate retry/active-progress checks. The Node24 script remains additional differential proof rather than the sole regression gate. Final locked Vitest run: 7 files, 81 tests pass (guard17, update-route4, shared33, run4, progress6, restart5, refs12). No production-code changes in this coverage loop.
