# capital-command

**All project instructions live in [`AGENTS.md`](./AGENTS.md). Read that file first
and follow it.**

`AGENTS.md` is the single source of truth for every coding agent working in this
repo (Claude Code, Codex, and anything else). This file exists only so Claude Code
picks the instructions up automatically. Do not duplicate rules here - if a rule
needs to change, change it in `AGENTS.md`.
